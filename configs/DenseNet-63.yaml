dataset:
  name: CIFAR10
  path: data/cifar10
  batch_size: 128
  num_workers: 4
  n_fixed_batches: 5

model:
  resnet: False        
  use_skip: True     
  num_config: "63"        
  activation: relu  
  dropout: 0     

training:
  epochs: 15
  lr: 0.1
  momentum: 0.9
  nesterov: true
  weight_decay: 0.0005
  device: cuda   
